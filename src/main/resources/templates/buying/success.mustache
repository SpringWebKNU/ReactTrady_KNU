{{>layouts/header}}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-light rounded">
                <div class="card-header text-center bg-success text-white">
                    <h3 class="m-0">주문 완료!</h3>
                </div>
                <div class="card-body">
                    <div class="text-center mb-4">
                        <h4 class="text-success">주문해 주셔서 감사합니다!</h4>
                        <p>주문이 정상적으로 처리되었습니다. 아래의 정보를 확인해 주세요.</p>
                    </div>

                    <div class="text-center mb-4">
                        <img src="/images/uploads/{{productImageUrl}}" alt="{{productName}}" class="img-fluid" style="max-width: 300px;">
                    </div>

                    <h5 class="fw-bold text-success">구매자</h5>
                    <p>{{userName}}님</p>

                    <h5 class="fw-bold text-success">이메일</h5>
                    <p>{{email}}</p>

                    <h5 class="fw-bold text-success">배송 주소</h5>
                    <p>{{address}}</p>

                    <h5 class="fw-bold text-success">상품명</h5>
                    <p>{{productName}}</p>

                    <h5 class="fw-bold text-success">사이즈</h5>
                    <p>{{size}}</p>

                    <h5 class="fw-bold text-success">가격</h5>
                    <p>{{price}}</p>

                    <h5 class="fw-bold text-success">구매 정보</h5>
                    <p>주문 번호: {{buying.id}}</p>

                    <div class="text-center mt-4">
                        <a href="/products/all" class="btn btn-primary">홈으로 가기</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{{>layouts/footer}}
